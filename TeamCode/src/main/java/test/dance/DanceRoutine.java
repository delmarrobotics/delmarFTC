package test.dance;

import common.Auto;
import common.Logger;
import common.Robot;
/*

//import javaFX
import java.io.File;
import javafx.scene.media.Media;
import javafx.scene.media.MediaPlayer;

import com.qualcomm.robotcore.eventloop.opmode.Autonomous;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcode.eventloop.opMode.Disabled;

import org.firstinspires.ftc.teamcode.drive.SampleMecanumDrive;

@Autonomous(name="Dance", group="Dance")
@Disabled
public class Dance_Op extends LinearOpMode {

        public void runOpMode() {
                // Initialize the robot hardware.
                Robot robot = new Robot(this);
                robot.init();

                SampleMecanumDrive drive = new SampleMecanumDrive(hardwareMap);

                waitForStart();

                //delete this if playing sound is against competition rules
                String bip = "sound.mp3";
                Media hit = new Media(new File(bip).toURI().toString());
                MediaPlayer mediaPlayer = new MediaPlayer(hit);
                mediaPlayer.play();
        }
}


 */